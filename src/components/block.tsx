import * as React from "react";
import * as ReactDOM from "react-dom";

const startImage = require("./cwebs_star.png");

export default function() {
    return (
        <img src={startImage} />
    );
}
