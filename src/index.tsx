import * as React from "react";
import * as ReactDOM from "react-dom";

import "./index.css";

ReactDOM.render(
    <div>
        This is 1c landing page
    </div>,
  document.getElementById("root") as HTMLElement,
);
